# Tools Service（独立工具引擎服务）运行环境变量示例。
# 在 packages/tools-service 内执行 npm run dev:node；或复制到仓库根 .env。

# 服务监听端口（Node / Docker）
# PORT=8899

# 可选：内部鉴权令牌。配置后，Proxy 调用本服务需携带
# Authorization: Bearer <TOOLS_SERVICE_TOKEN>；留空则跳过校验。
# TOOLS_SERVICE_TOKEN=