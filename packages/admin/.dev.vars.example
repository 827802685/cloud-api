# 本地 `npm run preview` / `npm run dev:admin` 时 Wrangler 会合并此文件（勿提交真实生产密码）。
# 若本文件不存在，`scripts/ensure-dev-vars.mjs` 会自动生成同名 `.dev.vars`（默认 admin / admin）。
# Cloudflare 运行时仅支持 D1（由 wrangler 绑定 DB），不要在这里配置 DATABASE_URL。
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
# 免费模型 RSS 同步源（可改域名；留空则用代码内置默认值）。
RSS_SYNC_URL=https://rss.zjkl.dpdns.org/rss.xml
# Workers AI 智能归类（可选）：配置后 RSS 同步会用 AI 复核模型分类。
# 方式一：在 wrangler.jsonc 配置 AI binding（env.AI）。
# 方式二：REST 通道，填 Cloudflare API Token + Account ID。
# CF_API_TOKEN=
# CF_ACCOUNT_ID=
